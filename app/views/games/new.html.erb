<% GameSession.create(user_id: current_user.id, game_id: @game.id ) %>
<% @game.add_user(current_user)%>
<% @game.init_game %>
<%# redirect to betting page here and then once the bet is placed redirect to game%>
<%# controller.redirect_to '/bet'%>
<% controller.redirect_to @game %>