<div class="betting">
  <% if current_user.tokens==0 %>
      <h1 style="font-size: 50px; margin-bottom: -50px;"> You have no more tokens! <br> <br> <h1>
      <h1 style="font-size: 40px; margin-bottom: -70px;"> Head to the store to earn more: <h1>
      <%= link_to "Store", store_path, style: "font-size: 30px; color:white; text-align: center", class: "btn btn-lg btn-success"%>
  <% else %>
    <h1 style="font-size: 60px; margin-bottom: 0px;">  Input your desired bet amount:<br> <h1>
    <h1 style="font-size: 20px; margin-bottom: -100px; color: whitesmoke;">  You must bet at least 10 tokens, but no greater than your current token amount<br> <h1>
    <%= label_tag(:q, "") %>
    <%= form_tag "/games/new", method: 'get' %>
    <div class = "play"">
    <br><br>
      <%= number_field_tag :q, 10, min: 10, max: current_user.tokens %>  
      <div style: >
      <%= submit_tag("Play!") %>
      <div>
    <div>

<%end%>
